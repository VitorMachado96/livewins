{
  "homepage": "https://srwins.github.io/livewins",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "http-server": "^14.1.1",
    "live-server": "^1.2.0"
  },
  "scripts": {
    "deploy": "gh-pages -d ./",
    "dev": "live-server --port=8080 --open=index.html"
  }
}
